# PalPeek Host — generated defaults
# The desktop app always submits a valid game PID/HWND before a stream starts.
capture = wgc
file_apps = apps.json
keyboard = disabled
mouse = disabled
controller = disabled
native_pen_touch = disabled
upnp = disabled
system_tray = disabled
origin_web_ui_allowed = pc
address_family = both
min_log_level = 2
